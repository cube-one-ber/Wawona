# Configuration for Skip (https://skip.dev) — WawonaUI is the SkipFuseUI surface:
# native Swift on Apple, Kotlin + Jetpack Compose on Android mirroring the same SwiftUI tree.
#
# Optional: add Android-only Gradle dependencies for this module, e.g.:
# build:
#   contents:
#     - block: 'dependencies'
#       contents:
#         - 'implementation("androidx.compose.runtime:runtime")'
skip:
  mode: native
